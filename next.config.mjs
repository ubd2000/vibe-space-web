/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    images: {
        unoptimized: true, // For simple deployment, or configure domains
    },
};

export default nextConfig;
